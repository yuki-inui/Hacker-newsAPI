import styled from "styled-components";

const HeaderJob = () => {
  <Job>Job</Job>;
};

const Job = styled.div`
  color: black;
  font-size: 10px;
  font-weigh: normal;
`;

export default HeaderJob;
