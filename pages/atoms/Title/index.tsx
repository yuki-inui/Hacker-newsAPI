import styled from "styled-components";

const Title = () => {
  <Titlefont>Hacker News</Titlefont>;
};

const Titlefont = styled.div`
  color: black;
  font-size: 20px;
  font-weigh: normal;
`;

export default Title;
