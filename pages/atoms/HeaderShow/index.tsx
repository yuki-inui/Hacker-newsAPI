import styled from "styled-components";

const HeaderShow = () => {
  <Show>Show</Show>;
};

const Show = styled.div`
  color: black;
  font-size: 10px;
  font-weigh: normal;
`;

export default HeaderShow;
