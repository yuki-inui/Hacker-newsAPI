import styled from "styled-components";

const HeaderTop = () => {
  <Top>Top</Top>;
};

const Top = styled.div`
  color: black;
  font-size: 10px;
  font-weigh: normal;
`;

export default HeaderTop;
